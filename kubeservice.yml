kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: estuary-discovery
  name: estuary-discovery
  namespace: default
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    k8s-app: estuary-discovery
